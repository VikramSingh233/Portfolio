import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
 
    images: {
      domains: ['example.com'], // Replace 'example.com' with your image host domain
    },
  
};

export default nextConfig;
