import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
 
    images: {
      domains: ['example.com','i0.wp.com','images.pexels.com','images.squarespace-cdn.com'], // Replace 'example.com' with your image host domain
    },
   
  
};

export default nextConfig;
